@{  
	var areas = ODataForTFS.GetAreaPaths();
    Response.ClearContent();
    Response.AppendHeader("Content-Type", "application/json");
    Response.Write(Json.Encode(areas));
    Response.End();
}
